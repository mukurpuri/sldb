
// Imports: Dependencies
import React from 'react';
function OnlyDesktop() {
  return (
    <React.Fragment>
        <div className="noTab">
            <div className="showDesktop">
                <p>SLDB works for Desktop only for now</p>
            </div>
        </div>
    </React.Fragment>
  );
}

export default OnlyDesktop;
